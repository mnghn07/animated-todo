// ? UNDETERMINED(0)
// ? FAILED(1)
// ? BEGAN(2)
// ? CANCELLED(3)
// ? ACTIVE(4)
// ? END(5)
export enum EVENT_TYPE {
  UNDETERMINED = 0,
  FAILED = 1,
  BEGAN = 2,
  CANCELLED = 3,
  ACTIVE = 4,
  END = 5,
}
